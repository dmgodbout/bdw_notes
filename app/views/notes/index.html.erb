<div class="notes">

<h1>BDW Notes</h1>
	
	<table>
	  <tr>
	    <th>Little Insights Into Our World</th>
	    <th></th>
	    <th></th>
	    <th></th>
	    <th></th>
	    <th></th>
	  </tr>
	
	<% @notes.each do |note| %>
	  <tr>
	    <td><%= note.name %></td>
	    <td><%= note.title %></td>
	    <td><%= note.content %></td>
	    <td><%= link_to 'Show', note %></td>
	    <td><%= link_to 'Edit', edit_note_path(note) %></td>
	    <td><%= link_to 'Destroy', note, confirm: 'Are you sure?', method: :delete %></td>
	  </tr>
	<% end %>
	</table>
	
	<br />
	
	<%= link_to 'New Note', new_note_path %></div>
